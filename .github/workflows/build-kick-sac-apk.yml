name: Build Kick Sac Fighter V7 APK

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: kick-sac-app
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - name: Clean and build V7 APK
        run: |
          gradle -p kick-sac-android clean app:assembleDebug
          cp kick-sac-android/app/build/outputs/apk/debug/app-debug.apk Kick-Sac-Fighter-V7.apk
      - name: Upload V7 APK
        uses: actions/upload-artifact@v4
        with:
          name: Kick-Sac-Fighter-V7
          path: Kick-Sac-Fighter-V7.apk
