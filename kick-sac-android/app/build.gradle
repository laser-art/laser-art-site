plugins { id 'com.android.application' }

android {
    namespace 'be.laserart.kicksac'
    compileSdk 35

    defaultConfig {
        applicationId 'be.laserart.kicksac.v4'
        minSdk 24
        targetSdk 35
        versionCode 4
        versionName '4.0.0'
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}
